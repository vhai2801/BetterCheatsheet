# Better Cheatsheet

## Overview
A native macOS menu-bar/window app for keeping notes on custom keyboard shortcuts. Notes are organized into user-named tabs (unlimited count) shown in a top tab bar. A global hotkey (user-configurable, default **⇧⌘K**) toggles a floating overlay panel that shows the same tabs/content as a quick-glance cheatsheet, without switching away from whatever app you're using. Tabs can individually be marked "editable in overlay" so you can jot things down without opening the full editor window. While editing, typing an exact ALL-CAPS modifier keyword (e.g. `CMD`, `OPTION`, `SHIFT`) auto-replaces it with its symbol (⌘, ⌥, ⇧, ...) as soon as you type a word-boundary character after it. A pinned "Settings" tab lets you re-record the global shortcut and pick a Light/Dark/Frosted-Glass theme applied to both the main window and the overlay. The app makes no network connections and stores notes locally only, at `~/Library/Application Support/BetterCheatsheet/tabs.json` (settings alongside it in `settings.json`).

Built as a Swift Package (not an Xcode project) because only Xcode Command Line Tools are installed, not full Xcode. `build.sh` wraps the compiled binary into a real, ad-hoc-codesigned `BetterCheatsheet.app` bundle.

## Architecture map
- `Package.swift` — executable target `BetterCheatsheet`, macOS 13+, zero dependencies
- `Sources/BetterCheatsheet/main.swift` — entry point: creates `NSApplication`, sets `.regular` activation policy, assigns `AppDelegate`, calls `app.run()`
- `Sources/BetterCheatsheet/AppDelegate.swift` — owns the main editor `NSWindow`, the floating `OverlayPanel` (an `NSPanel` subclass with `.nonactivatingPanel` style so it can become key for typing without stealing app focus), the `NSStatusItem` menu bar icon/menu (Show Cheatsheet / Edit Notes / Quit), and registers the global hotkey
- `Sources/BetterCheatsheet/HotKeyManager.swift` — wraps Carbon's `RegisterEventHotKey`/`InstallEventHandler`. Chosen over a `CGEventTap` specifically to avoid needing Accessibility/Input Monitoring permission on first launch
- `Sources/BetterCheatsheet/Models.swift` — `TabItem` (Codable: id, name, content, editableInOverlay) and `AppState` (ObservableObject; loads/saves `[TabItem]` as JSON to Application Support, persists selected tab id in `UserDefaults`)
- `Sources/BetterCheatsheet/TextReplacement.swift` — pure dictionary (ALL-CAPS keyword → symbol) + `TextReplacement.replacement(in:beforeLocation:)`, a pure NSString range-finder with no UIKit/AppKit dependency, easy to unit-test/extend
- `Sources/BetterCheatsheet/AutoReplaceTextEditor.swift` — `NSViewRepresentable` wrapping `NSTextView`; intercepts `shouldChangeTextIn:replacementString:` to apply `TextReplacement` live at the caret, preserving cursor position
- `Sources/BetterCheatsheet/TabBarView.swift` — horizontal scrolling tab bar; `TabButton` collapses to `maxWidth: 110` and expands to fit content on hover (animated); double-click to rename, right-click for rename/delete, "+" control to add (hidden in the overlay via `allowAdding: false`)
- `Sources/BetterCheatsheet/EditorView.swift` — main window content: tab bar + "editable in overlay" checkbox + `AutoReplaceTextEditor` for the selected tab, or `SettingsView` when the pinned Settings tab is selected (`appState.isShowingSettings`)
- `Sources/BetterCheatsheet/CheatsheetView.swift` — overlay content: tab bar (no add control) + read-only `Text` unless the tab is `editableInOverlay`, in which case the same `AutoReplaceTextEditor` is used
- `Sources/BetterCheatsheet/Settings.swift` — `AppTheme` enum (`light`/`dark`/`frostedGlass`), `HotKeyConfig` struct, `SettingsStore` (ObservableObject, persists both to `settings.json`)
- `Sources/BetterCheatsheet/HotKeyFormatter.swift` — pure logic: `HotKeyConfig` → display string (e.g. "⇧⌘K"), resolves the base character for a keyCode via `UCKeyTranslate` so it's correct regardless of keyboard layout
- `Sources/BetterCheatsheet/HotKeyRecorderView.swift` — click-to-record `NSViewRepresentable`; captures the next keyDown with at least one modifier held, Esc cancels
- `Sources/BetterCheatsheet/VisualEffectBackground.swift` — `NSVisualEffectView` wrapper (`.behindWindow` blending) used for the real frosted-glass look; the hosting window must be non-opaque with a clear background color for the blur to show through
- `Sources/BetterCheatsheet/TabBarView.swift` — also renders a pinned, non-deletable "⚙ Settings" pill (`showsSettingsTab`) alongside the user's content tabs, wired to `appState.isShowingSettings`
- `Info.plist` — app bundle metadata (`LSMinimumSystemVersion` 13.0, no network entitlements requested)
- `build.sh` — `swift build` + manual `.app` bundle assembly + ad-hoc `codesign`

## Current status (as of 2026-07-05)
- Everything built and verified: `swift build` succeeds, `build.sh` produces a working `.app`, launched and confirmed via screenshot — main window (tab bar, "+", checkbox), the pinned Settings tab (shortcut recorder showing ⇧⌘K, Light/Dark/Frosted-Glass picker) all render correctly.
- Git repo initialized at `~/Projects/BetterCheatsheet` (independent of the user's home-directory repo — that one was never touched), initial commit made, pushed to a new private GitHub repo: `https://github.com/vhai2801/BetterCheatsheet`.
- Not yet done / not verified by me: actually typing `CMD ` to confirm live auto-replace end-to-end, recording a new hotkey via the recorder and confirming it re-registers, confirming the overlay hotkey toggle doesn't steal focus, visually confirming Dark/Frosted-Glass themes apply (code path is straightforward — `NSApp.appearance` switch + conditional `VisualEffectBackground` — but not clicked through). These need a human clicking the real app; scripted GUI automation from the CLI was tried and abandoned as too risky (one attempt clicked the wrong window because of a screen-coordinate mismatch).
- Everything below "Build & repo steps" in the original plan (`/Users/blub/.claude/plans/melodic-floating-crane.md`) is the source of truth for what was originally scoped, in case this doc and that plan ever disagree. The Settings tab (shortcut recorder + theme picker) was added after that plan was approved, based on a follow-up request, and is documented here instead of in the plan file.

## Next steps
1. Have the user click through: type `CMD ` in a tab and confirm `⌘ ` appears; press the recorded hotkey to confirm the overlay toggles without stealing focus from whatever app was frontmost; record a new hotkey in Settings and confirm the old one stops working and the new one takes over; try all three themes on both the main window and the overlay.
2. Fix anything reported broken from step 1.
3. Consider whether the "hello" tab (created during manual testing) should be deleted, renamed, or left as-is — it's real persisted user data now, not a bug.

## Decisions log
- 2026-07-05: Chose Swift Package Manager + a manual `.app`-bundle build script over an Xcode project, because only Command Line Tools are installed (no `xcodebuild`). Revisit if the user installs full Xcode and wants project-file conveniences (asset catalogs for a real app icon, etc.).
- 2026-07-05: Chose Carbon `RegisterEventHotKey` over a `CGEventTap`/`NSEvent.addGlobalMonitorForEvents` for the global hotkey specifically to avoid an Accessibility/Input Monitoring permission prompt on first launch. The Settings-tab recorder re-registers by discarding and recreating the `HotKeyManager` instance (its `deinit` unregisters the old binding) rather than adding an update method to Carbon's registration.
- 2026-07-05: Overlay is an `NSPanel` subclass (`OverlayPanel`) overriding `canBecomeKey` to allow text editing in overlay-editable tabs, while `.nonactivatingPanel` keeps it from stealing focus/activating the app when just glancing at a read-only tab.
- 2026-07-05: User notes (`tabs.json`) are stored per-machine in Application Support, not committed to git — only the app's source syncs across laptops. Revisit if the user wants notes portable across machines too (would need a manual export/import or committing the JSON, since there's no network sync by design).
- 2026-07-05: Theme is a single 3-way choice (Light/Dark/Frosted Glass), not independent appearance+material toggles, per the user's phrasing ("theme to dark or bright or frosted glass"). Frosted Glass sets `NSApp.appearance = nil` (follows system) and makes both windows non-opaque with a real `NSVisualEffectView` blur; Light/Dark force `NSApp.appearance` and keep windows opaque.
- 2026-07-05: Settings is a pinned tab inside the same tab bar as content tabs (not a separate window or sidebar), per the user's framing of "2 tabs: one text-edit, one settings." It's excluded from the overlay's tab bar (`showsSettingsTab` defaults to `false`) since the overlay is meant for glancing at notes, not configuration.
